@model List<ClassificaProdotto>

    @foreach(ClassificaProdotto categoria in Model)
                        {
                           <h1>@categoria</h1> 
                           <marquee behavior="scroll" scrollamount="3" direction="left" width="350">
                                <img class="oggetto" src="@categoria" alt="">
                                <h5 class="text">@categoria.Nome</h5> 
                         </marquee>
                        }
